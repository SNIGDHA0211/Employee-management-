/**
 * Production backend URL
 * @see https://employee-management-system-tmrl.onrender.com
 */
export const PRODUCTION_URL = 'https://employee-management-system-tmrl.onrender.com';

/**
 * When false, use ui-avatars.com for all profile images to avoid 429/503 from backend rate limiting.
 * Set to true when backend has sufficient rate limits (e.g. paid tier).
 */
export const USE_BACKEND_AVATARS = false;
